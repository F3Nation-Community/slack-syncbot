AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: >
  SyncBot - Slack app that syncs posts and replies across workspaces.
  DatabaseMode existing (default): TiDB / public MySQL or Postgres you already created.
  DatabaseMode sqlite: /tmp SQLite + Litestream replica on S3 (reserved concurrency 1).
  This stack does not create RDS or a VPC. SAM deploy uses an S3 artifact bucket for packaging.

Globals:
  Function:
    Timeout: 10
    MemorySize: 128
    Tracing: !If [XRayEnabled, Active, PassThrough]

# ================================================================
# Parameters
# ================================================================
# Grouping: Stage/mode/engine → Slack → Database (app user) → app secrets → app settings.

Parameters:
  Stage:
    Description: Deployment stage
    Type: String
    Default: test
    AllowedValues:
      - test
      - prod

  DatabaseMode:
    Description: >
      existing (default) = connect to a database you already created (TiDB, public MySQL, Postgres).
      sqlite = Lambda /tmp file + Litestream replica in S3. Do not leave ExistingDatabaseHost empty
      to "create a database"; empty host is an error in existing mode.
    Type: String
    Default: existing
    AllowedValues:
      - existing
      - sqlite

  DatabaseEngine:
    Description: >
      SQL engine for DatabaseMode=existing. Ignored for sqlite (runtime always sqlite).
      mysql (default) or postgresql.
    Type: String
    Default: mysql
    AllowedValues:
      - postgresql
      - mysql

  EnableKeepWarm:
    Description: >
      EventBridge ScheduleV2 keep-warm (ENABLE_KEEP_WARM). Portable deploy knob, not app env.
      true = ENABLED, false = DISABLED. Default true.
    Type: String
    Default: "true"
    AllowedValues:
      - "true"
      - "false"

  # --- Slack ---

  SlackSigningSecret:
    Description: Slack signing secret for request verification (SLACK_SIGNING_SECRET)
    Type: String
    NoEcho: true

  SlackClientID:
    Description: >
      Slack OAuth app Client ID (SLACK_CLIENT_ID; Basic Information → App Credentials).
      Required for your Slack app; use the ID from the app you created for this deploy.
    Type: String

  SlackClientSecret:
    Description: Slack OAuth client secret (SLACK_CLIENT_SECRET)
    Type: String
    NoEcho: true

  SlackOauthBotScopes:
    Description: Comma-separated list of Slack OAuth bot scopes (SLACK_BOT_SCOPES)
    Type: String
    Default: "app_mentions:read,channels:history,channels:join,channels:read,channels:manage,chat:write,chat:write.customize,files:read,files:write,groups:history,groups:read,groups:write,im:write,reactions:read,reactions:write,team:read,users:read,users:read.email"

  SlackOauthUserScopes:
    Description: >
      Comma-separated Slack OAuth user scopes (SLACK_USER_SCOPES). Must match slack-manifest.json
      oauth_config.scopes.user and syncbot/slack_manifest_scopes.py USER_SCOPES (same order).
    Type: String
    Default: "chat:write,channels:history,channels:read,files:read,files:write,groups:history,groups:read,groups:write,im:write,reactions:read,reactions:write,team:read,users:read,users:read.email"

  # --- Database: app credentials (existing mode) ---

  DatabaseUser:
    Description: >
      App database username (DATABASE_USER). Required when DatabaseMode=existing.
      Use the full username (including any TiDB cluster prefix). Unused for sqlite.
    Type: String
    Default: ""

  DatabasePassword:
    Description: >
      App database password (DATABASE_PASSWORD). Required when DatabaseMode=existing.
      Unused for sqlite.
    Type: String
    NoEcho: true
    Default: ""

  DataEncryptionKey:
    Description: >
      Passphrase for Fernet data-at-rest encryption (DATA_ENCRYPTION_KEY).
      Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(36))"
    Type: String
    NoEcho: true

  ExistingDatabaseHost:
    Description: >
      Hostname of an existing database (DATABASE_HOST), e.g. gateway.tidbcloud.com.
      Required when DatabaseMode=existing. Unused for sqlite.
    Type: String
    Default: ""

  ExistingDatabasePort:
    Description: >
      TCP port for ExistingDatabaseHost (DATABASE_PORT). Leave empty to use engine default
      (3306 MySQL, 5432 PostgreSQL). Set for providers with a non-standard port (e.g. TiDB Cloud 4000).
      Unused for sqlite.
    Type: String
    Default: ""

  DatabaseSchema:
    Description: >
      Database/schema name for MySQL or PostgreSQL when DatabaseMode=existing.
      Each app sharing a DB instance should use a different schema name. Unused for sqlite.
    Type: String
    Default: "syncbot"

  # --- App settings ---

  RequireAdmin:
    Description: >
      When "true" (default), only workspace admins and owners can
      configure SyncBot.  Set to "false" to allow any user.
    Type: String
    Default: "true"
    AllowedValues:
      - "true"
      - "false"

  LogLevel:
    Description: >
      Python logging level for the app (LOG_LEVEL). DEBUG, INFO, WARNING, ERROR, or CRITICAL.
    Type: String
    Default: INFO
    AllowedValues:
      - DEBUG
      - INFO
      - WARNING
      - ERROR
      - CRITICAL

  SoftDeleteRetentionDays:
    Description: Days to retain soft-deleted workspace data (SOFT_DELETE_RETENTION_DAYS).
    Type: Number
    Default: 30
    MinValue: 1

  SyncbotFederationEnabled:
    Description: Set to "true" to enable external connections / federation (SYNCBOT_FEDERATION_ENABLED).
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"

  SyncbotInstanceId:
    Description: >
      Optional stable UUID for this instance (SYNCBOT_INSTANCE_ID). Leave empty to auto-generate at runtime.
    Type: String
    Default: ""

  SyncbotPublicUrl:
    Description: >
      Public HTTPS base URL without path (SYNCBOT_PUBLIC_URL). Required when
      federation is enabled (SyncbotFederationEnabled=true); use the Function URL
      from stack outputs or a custom domain. Leave empty otherwise.
    Type: String
    Default: ""

  PrimaryWorkspace:
    Description: >
      Slack Team ID for PRIMARY_WORKSPACE. Required for backup/restore to appear; also scopes DB reset. Leave empty to hide backup/restore.
    Type: String
    Default: ""

  EnableDbReset:
    Description: >
      Set to "true" to enable Reset Database when PRIMARY_WORKSPACE matches (ENABLE_DB_RESET). Leave empty to disable.
      Legacy values (e.g. a Slack Team ID) are ignored by the app until updated to "true".
    Type: String
    Default: ""

  DatabaseTlsEnabled:
    Description: >
      Optional DATABASE_TLS_ENABLED. Empty = use app default (TLS on outside local dev).
      Set "true" or "false" to override. Unused for sqlite.
    Type: String
    Default: ""
    AllowedValues:
      - ""
      - "true"
      - "false"

  DatabaseSslCaPath:
    Description: >
      Optional CA bundle path when DB TLS is on (DATABASE_SSL_CA_PATH). Empty = app default.
      Unused for sqlite.
    Type: String
    Default: ""

  EnableXRay:
    Description: >
      Enable AWS X-Ray tracing on Lambda (extra cost beyond free tier).
      Set ENABLE_XRAY=true in deploy env to turn on.
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"

# ================================================================
# Conditions
# ================================================================

Conditions:
  IsSqlite: !Equals [!Ref DatabaseMode, sqlite]
  UseExistingDatabase: !Equals [!Ref DatabaseMode, existing]
  IsMysqlEngine: !Equals [!Ref DatabaseEngine, mysql]
  UseExistingDatabaseWithCustomPort: !And
    - !Condition UseExistingDatabase
    - !Not [!Equals [!Ref ExistingDatabasePort, ""]]
  KeepWarmEnabled: !Equals [!Ref EnableKeepWarm, "true"]
  HasSyncbotPublicUrlOverride: !Not [!Equals [!Ref SyncbotPublicUrl, ""]]
  HasPrimaryWorkspace: !Not [!Equals [!Ref PrimaryWorkspace, ""]]
  HasEnableDbReset: !Equals [!Ref EnableDbReset, "true"]
  HasDatabaseTlsExplicit: !And
    - !Condition UseExistingDatabase
    - !Not [!Equals [!Ref DatabaseTlsEnabled, ""]]
  HasDatabaseSslCaPath: !And
    - !Condition UseExistingDatabase
    - !Not [!Equals [!Ref DatabaseSslCaPath, ""]]
  XRayEnabled: !Equals [!Ref EnableXRay, "true"]

Rules:
  ExistingRequiresHost:
    RuleCondition: !Equals [!Ref DatabaseMode, existing]
    Assertions:
      - Assert: !Not [!Equals [!Ref ExistingDatabaseHost, ""]]
        AssertDescription: ExistingDatabaseHost is required when DatabaseMode is existing.
      - Assert: !Not [!Equals [!Ref DatabaseUser, ""]]
        AssertDescription: DatabaseUser is required when DatabaseMode is existing.
      - Assert: !Not [!Equals [!Ref DatabasePassword, ""]]
        AssertDescription: DatabasePassword is required when DatabaseMode is existing.

Mappings:
  StagesMap:
    test:
      KeepWarmName: "SyncBotKeepWarmTest"
    prod:
      KeepWarmName: "SyncBotKeepWarmProd"

Resources:
  # ============================================================
  # Litestream replica bucket (sqlite mode)
  # ============================================================

  LitestreamBucket:
    Type: AWS::S3::Bucket
    Condition: IsSqlite
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Sub "syncbot-${Stage}-litestream-${AWS::AccountId}"
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
          - Id: KeepLastFewVersions
            Status: Enabled
            NoncurrentVersionExpiration:
              NoncurrentDays: 1
              NewerNoncurrentVersions: 3
            ExpiredObjectDeleteMarker: true
          - Id: AbortIncompleteUploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 1

  # ============================================================
  # Lambda Function
  # ============================================================

  SyncBotFunction:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: makefile
      cfn-lint:
        config:
          ignore_checks:
            - W1030
    Properties:
      FunctionUrlConfig:
        AuthType: NONE
      CodeUri: lambda/
      Handler: handler.handler
      Runtime: python3.12
      Architectures:
        - x86_64
      Timeout: 30
      MemorySize: 256
      ReservedConcurrentExecutions: !If [IsSqlite, 1, !Ref AWS::NoValue]
      Events:
        SyncBotKeepWarm:
          Type: ScheduleV2
          Properties:
            ScheduleExpression: "rate(5 minutes)"
            Name: !FindInMap
              - StagesMap
              - !Ref Stage
              - KeepWarmName
            State: !If [KeepWarmEnabled, ENABLED, DISABLED]
      Environment:
        Variables:
          SLACK_BOT_TOKEN: "123"
          SLACK_SIGNING_SECRET: !Ref SlackSigningSecret
          SLACK_CLIENT_SECRET: !Ref SlackClientSecret
          SLACK_BOT_SCOPES: !Ref SlackOauthBotScopes
          SLACK_USER_SCOPES: !Ref SlackOauthUserScopes
          SLACK_CLIENT_ID: !Ref SlackClientID
          DATABASE_BACKEND: !If [IsSqlite, sqlite, !Ref DatabaseEngine]
          DATABASE_URL: !If [IsSqlite, "sqlite:////tmp/syncbot.db", !Ref AWS::NoValue]
          LITESTREAM_S3_BUCKET: !If [IsSqlite, !Ref LitestreamBucket, !Ref AWS::NoValue]
          DATABASE_PORT: !If
            - UseExistingDatabaseWithCustomPort
            - !Ref ExistingDatabasePort
            - !If
              - UseExistingDatabase
              - !If
                - IsMysqlEngine
                - "3306"
                - "5432"
              - !Ref AWS::NoValue
          DATABASE_HOST: !If [UseExistingDatabase, !Ref ExistingDatabaseHost, !Ref AWS::NoValue]
          DATABASE_USER: !If [UseExistingDatabase, !Ref DatabaseUser, !Ref AWS::NoValue]
          DATABASE_PASSWORD: !If [UseExistingDatabase, !Ref DatabasePassword, !Ref AWS::NoValue]
          DATABASE_SCHEMA: !If [UseExistingDatabase, !Ref DatabaseSchema, !Ref AWS::NoValue]
          DATA_ENCRYPTION_KEY: !Ref DataEncryptionKey
          REQUIRE_ADMIN: !Ref RequireAdmin
          LOG_LEVEL: !Ref LogLevel
          SOFT_DELETE_RETENTION_DAYS: !Sub "${SoftDeleteRetentionDays}"
          SYNCBOT_FEDERATION_ENABLED: !Ref SyncbotFederationEnabled
          SYNCBOT_INSTANCE_ID: !Ref SyncbotInstanceId
          SYNCBOT_PUBLIC_URL: !Ref SyncbotPublicUrl
          PRIMARY_WORKSPACE: !If
            - HasPrimaryWorkspace
            - !Ref PrimaryWorkspace
            - !Ref AWS::NoValue
          ENABLE_DB_RESET: !If
            - HasEnableDbReset
            - "true"
            - !Ref AWS::NoValue
          DATABASE_TLS_ENABLED: !If
            - HasDatabaseTlsExplicit
            - !Ref DatabaseTlsEnabled
            - !Ref AWS::NoValue
          DATABASE_SSL_CA_PATH: !If
            - HasDatabaseSslCaPath
            - !Ref DatabaseSslCaPath
            - !Ref AWS::NoValue

  # Slack Bolt (aws_lambda adapter) runs lazy listeners by invoking this function again
  # via lambda:InvokeFunction. The execution role must allow self-invoke.
  SyncBotFunctionSelfInvokePolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: !Sub "syncbot-${Stage}-self-invoke"
      Roles:
        - !Ref SyncBotFunctionRole
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowSelfInvokeForBoltLazyListeners
            Effect: Allow
            Action:
              - lambda:InvokeFunction
            Resource: !GetAtt SyncBotFunction.Arn

  SyncBotLitestreamS3Policy:
    Type: AWS::IAM::Policy
    Condition: IsSqlite
    Properties:
      PolicyName: !Sub "syncbot-${Stage}-litestream-s3"
      Roles:
        - !Ref SyncBotFunctionRole
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: LitestreamListBucket
            Effect: Allow
            Action:
              - s3:ListBucket
            Resource: !GetAtt LitestreamBucket.Arn
          - Sid: LitestreamObjects
            Effect: Allow
            Action:
              - s3:GetObject
              - s3:PutObject
              - s3:DeleteObject
            Resource: !Sub "${LitestreamBucket.Arn}/*"

  # ============================================================
  # CloudWatch Logs and Alarms
  # ============================================================

  SyncBotLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${SyncBotFunction}"
      RetentionInDays: 30

  LambdaErrorAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "syncbot-${Stage}-lambda-errors"
      AlarmDescription: >
        Fires when the SyncBot Lambda function produces 3 or more
        errors within a 5-minute window.
      Namespace: AWS/Lambda
      MetricName: Errors
      Dimensions:
        - Name: FunctionName
          Value: !Ref SyncBotFunction
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 3
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching

  LambdaThrottleAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "syncbot-${Stage}-lambda-throttles"
      AlarmDescription: >
        Fires when the SyncBot Lambda function is throttled.
      Namespace: AWS/Lambda
      MetricName: Throttles
      Dimensions:
        - Name: FunctionName
          Value: !Ref SyncBotFunction
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching

  LambdaDurationAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "syncbot-${Stage}-lambda-duration"
      AlarmDescription: >
        Fires when average Lambda duration exceeds 10 seconds.
      Namespace: AWS/Lambda
      MetricName: Duration
      Dimensions:
        - Name: FunctionName
          Value: !Ref SyncBotFunction
      Statistic: Average
      Period: 300
      EvaluationPeriods: 2
      Threshold: 10000
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching

# ================================================================
# Outputs
# ================================================================

Outputs:
  SyncBotPublicBaseUrl:
    Description: Public HTTPS base URL (SYNCBOT_PUBLIC_URL) for Slack and federation
    Value: !If
      - HasSyncbotPublicUrlOverride
      - !Ref SyncbotPublicUrl
      - !GetAtt SyncBotFunctionUrl.FunctionUrl

  SyncBotApiUrl:
    Description: Lambda Function URL endpoint for Slack events
    Value: !Sub "${SyncBotFunctionUrl.FunctionUrl}slack/events"

  SyncBotInstallUrl:
    Description: Slack app installation URL
    Value: !Sub "${SyncBotFunctionUrl.FunctionUrl}slack/install"

  SyncBotFunctionArn:
    Description: SyncBot Lambda function ARN
    Value: !GetAtt SyncBotFunction.Arn

  DatabaseHostInUse:
    Description: Database host the Lambda is configured to connect to (or sqlite for Litestream mode)
    Value: !If
      - IsSqlite
      - sqlite
      - !Ref ExistingDatabaseHost

  LitestreamBucketName:
    Condition: IsSqlite
    Description: S3 bucket for Litestream SQLite replica
    Value: !Ref LitestreamBucket
